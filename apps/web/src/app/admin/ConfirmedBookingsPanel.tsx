"use client";
import {useEffect,useMemo,useState} from "react";
import {createPortal} from "react-dom";
import {formatStayRange} from "@/lib/date-format";
import {DepositControls} from "./DepositControls";
import type {AdminEnquiry} from "./BookingRequestsPanel";
import styles from "./admin.module.css";
import {AdminPriceReceipt} from "./AdminPriceReceipt";
import {BalanceControls} from "./BalanceControls";
const PAGE_SIZE=6;
export function ConfirmedBookingsPanel({bookings,canManage}:{bookings:AdminEnquiry[];canManage:boolean}){const [query,setQuery]=useState("");const [page,setPage]=useState(1);const [selectedId,setSelectedId]=useState<string|null>(null);const selected=selectedId?bookings.find(item=>item.id===selectedId)??null:null;const confirmed=useMemo(()=>bookings.filter(item=>item.status==="confirmed"&&[item.fullName,item.phone,item.email,item.depositReference].some(value=>value?.toLowerCase().includes(query.toLowerCase()))),[bookings,query]);const pageCount=Math.max(1,Math.ceil(confirmed.length/PAGE_SIZE));const currentPage=Math.min(page,pageCount);const visible=confirmed.slice((currentPage-1)*PAGE_SIZE,currentPage*PAGE_SIZE);
useEffect(()=>{if(!selected)return;const previous=document.body.style.overflow;const close=(event:KeyboardEvent)=>{if(event.key==="Escape")setSelectedId(null);};document.body.style.overflow="hidden";window.addEventListener("keydown",close);return()=>{document.body.style.overflow=previous;window.removeEventListener("keydown",close);};},[selected]);
return <section className={styles.panel}><div className={styles.panelHeading}><div><p className={styles.eyebrow}>Confirmed stays</p><h2>Confirmed bookings</h2></div><span className={styles.countBadge}>{confirmed.length} confirmed</span></div><div className={styles.bookingFilters}><label><span>Search confirmed bookings</span><input type="search" value={query} onChange={event=>{setQuery(event.target.value);setPage(1);}} placeholder="Guest, phone, email, or transfer reference"/></label></div>{visible.length?<><div className={styles.bookingCardGrid}>{visible.map(item=><article className={styles.bookingSummaryCard} key={item.id}><div><strong>{item.fullName}</strong><span>{formatStayRange(item.checkIn,item.checkOut)}</span></div><dl><div><dt>Guests</dt><dd>{item.guestCount}</dd></div><div><dt>Payment</dt><dd>{item.remainingBalanceMinor===0?"Fully paid":"Balance due"}</dd></div><div><dt>Remaining</dt><dd>₱{(item.remainingBalanceMinor/100).toLocaleString("en-PH")}</dd></div></dl><button type="button" onClick={()=>setSelectedId(item.id)}>View booking details</button></article>)}</div>{pageCount>1?<nav className={styles.pagination} aria-label="Confirmed bookings pages"><button type="button" disabled={currentPage===1} onClick={()=>setPage(value=>Math.max(1,value-1))}>Previous</button><span>Page {currentPage} of {pageCount}</span><button type="button" disabled={currentPage===pageCount} onClick={()=>setPage(value=>Math.min(pageCount,value+1))}>Next</button></nav>:null}</>:<div className={styles.emptyState}><span aria-hidden="true">✓</span><h3>No confirmed bookings found</h3><p>Verified bookings automatically move here from the request queue.</p></div>}{selected?createPortal(<div className={styles.bookingModalBackdrop} role="presentation" onMouseDown={event=>{if(event.target===event.currentTarget)setSelectedId(null);}}><section className={styles.bookingInfoModal} role="dialog" aria-modal="true" aria-labelledby="confirmed-booking-title"><header><div><p className={styles.eyebrow}>Confirmed booking</p><h2 id="confirmed-booking-title">{selected.fullName}</h2></div><button type="button" aria-label="Close booking details" onClick={()=>setSelectedId(null)}>×</button></header><div className={styles.bookingModalBody}><dl className={styles.bookingInfoGrid}><div><dt>Stay</dt><dd>{formatStayRange(selected.checkIn,selected.checkOut)}</dd></div><div><dt>Guests</dt><dd>{selected.guestCount}</dd></div><div><dt>Phone</dt><dd>{selected.phone}</dd></div><div><dt>Email</dt><dd>{selected.email||"Not provided"}</dd></div><div><dt>Bedroom</dt><dd>{selected.bedroomChoice.replaceAll("_"," ")}</dd></div><div><dt>Deposit</dt><dd>{selected.depositStatus.replaceAll("_"," ")}</dd></div><div><dt>Sender/account</dt><dd>{selected.depositSenderName||"Not provided"}</dd></div><div><dt>Reference</dt><dd>{selected.depositReference||"Messenger receipt"}</dd></div></dl><AdminPriceReceipt booking={selected}/><div className={styles.bookingModalActions}><a className={styles.callGuestButton} href={`tel:${selected.phone}`}>Call guest</a>{selected.email?<a href={`mailto:${selected.email}`}>Email guest</a>:null}</div><div className={styles.bookingModalControls}><DepositControls bookingId={selected.id} bookingStatus={selected.status} depositStatus={selected.depositStatus} canManage={canManage}/><BalanceControls bookingId={selected.id} remainingBalanceMinor={selected.remainingBalanceMinor} canManage={canManage}/></div></div></section></div>,document.body):null}</section>}
